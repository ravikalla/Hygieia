dbname=dashboarddb
dbhost=localhost
dbport=27017
dbusername=dashboarduser
dbpassword=dbpassword

# Collector schedule (required)
jenkins.cron=0 0/5 * * * *

# The page size
jenkins.pageSize=1000

# Jenkins server (required) - Can provide multiple
jenkins.servers[0]=http://localhost:8005

# If using username/token for api authentication
#   (required for Cloudbees Jenkins Ops Center) see sample
jenkins.servers[1]=http://ravi:3c42faeca78ea8d7e43f717969cdb987@localhost:8005

# Another option: If using same username/password Jenkins auth,
#   set username/apiKey to use HTTP Basic Auth (blank=no auth)
jenkins.usernames[0]=ravi
jenkins.apiKeys[0]=3c42faeca78ea8d7e43f717969cdb987

# Determines if build console log is collected - defaults to false
jenkins.saveLog=true